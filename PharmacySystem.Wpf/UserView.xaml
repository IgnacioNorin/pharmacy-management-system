<UserControl x:Class="PharmacySystem.Ui.UserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource AppCanvasBrush}">

    <DockPanel Margin="24">

        <TextBlock DockPanel.Dock="Top" Text="Usuarios" FontSize="22" FontWeight="Bold"
                   Margin="0,0,0,16"/>

        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Buscar:" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <TextBox x:Name="txtSearch" Width="240" Height="24"/>
            <Button x:Name="btnSearch" Content="Buscar" Margin="8,0,0,0" Click="btnSearch_Click"/>
            <Button x:Name="btnClearSearch" Content="Limpiar" Margin="6,0,0,0" Click="btnClearSearch_Click"/>
        </StackPanel>

        <GroupBox DockPanel.Dock="Bottom" Header="Datos del usuario" Padding="8" Margin="0,10,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.Resources>
                    <Style TargetType="TextBlock"><Setter Property="VerticalAlignment" Value="Center"/><Setter Property="Margin" Value="0,4,8,4"/></Style>
                    <Style TargetType="TextBox"><Setter Property="Margin" Value="0,4,16,4"/></Style>
                    <Style TargetType="PasswordBox"><Setter Property="Margin" Value="0,4,16,4"/></Style>
                </Grid.Resources>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Documento"/>
                <TextBox   Grid.Row="0" Grid.Column="1" x:Name="txtDocument"/>
                <TextBlock Grid.Row="0" Grid.Column="2" Text="Nombre completo"/>
                <TextBox   Grid.Row="0" Grid.Column="3" x:Name="txtName"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Contraseña"/>
                <PasswordBox Grid.Row="1" Grid.Column="1" x:Name="txtPassword"/>
                <TextBlock Grid.Row="1" Grid.Column="2" Text="Confirmar contraseña"/>
                <PasswordBox Grid.Row="1" Grid.Column="3" x:Name="txtConfirm"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Rol"/>
                <ComboBox  Grid.Row="2" Grid.Column="1" x:Name="cboRole" Margin="0,4,16,4"
                           DisplayMemberPath="Text"/>
                <StackPanel Grid.Row="2" Grid.Column="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,4">
                    <Button x:Name="btnSave" Content="Guardar" Click="btnSave_Click"/>
                    <Button x:Name="btnClearForm" Content="Limpiar" Margin="8,0,0,0" Click="btnClearForm_Click"/>
                    <Button x:Name="btnDelete" Content="Eliminar" Margin="8,0,0,0" Click="btnDelete_Click"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <GroupBox Header="Listado de usuarios" Padding="8">
        <DataGrid x:Name="dgUsers"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  CanUserAddRows="False" HeadersVisibility="Column"
                  SelectionMode="Single" GridLinesVisibility="Horizontal" RowHeaderWidth="0"
                  Background="White" RowBackground="White" BorderThickness="0"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                  SelectionChanged="dgUsers_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Documento" Width="1.2*" Binding="{Binding Document}"/>
                <DataGridTextColumn Header="Nombre" Width="2*" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Rol" Width="1.4*" Binding="{Binding RoleText}"/>
                <DataGridTextColumn Header="Estado" Width="1*" Binding="{Binding StatusText}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="SeaGreen"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding StatusText}" Value="Bloqueado">
                                    <Setter Property="Foreground" Value="Firebrick"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding StatusText}" Value="Inactivo">
                                    <Setter Property="Foreground" Value="Gray"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTemplateColumn Header="" Width="110">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Acciones…" Padding="8,2" MinHeight="0" Click="btnActions_Click"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        </GroupBox>
    </DockPanel>
</UserControl>
