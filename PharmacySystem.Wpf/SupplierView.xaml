<UserControl x:Class="PharmacySystem.Ui.SupplierView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource AppCanvasBrush}">

    <DockPanel Margin="24">

        <TextBlock DockPanel.Dock="Top" Text="Proveedores" FontSize="22" FontWeight="Bold"
                   Margin="0,0,0,16"/>

        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Buscar:" VerticalAlignment="Center" Margin="0,0,6,0"/>
            <TextBox x:Name="txtSearch" Width="260" Height="24"
                     ToolTip="Busca por razón social, documento o correo"/>
            <Button x:Name="btnSearch" Content="Buscar" Margin="8,0,0,0" Click="btnSearch_Click"/>
            <Button x:Name="btnClearSearch" Content="Limpiar" Margin="6,0,0,0" Click="btnClearSearch_Click"/>
        </StackPanel>

        <GroupBox DockPanel.Dock="Bottom" Header="Detalle del proveedor" Margin="0,10,0,0" Padding="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.Resources>
                    <Style TargetType="TextBlock">
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="Margin" Value="0,4,8,4"/>
                    </Style>
                    <Style TargetType="TextBox">
                        <Setter Property="Margin" Value="0,4,16,4"/>
                    </Style>
                </Grid.Resources>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Número Documento"/>
                <TextBox   Grid.Row="0" Grid.Column="1" x:Name="txtDocument"/>
                <TextBlock Grid.Row="0" Grid.Column="2" Text="Razón Social"/>
                <TextBox   Grid.Row="0" Grid.Column="3" x:Name="txtCompanyName"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Correo"/>
                <TextBox   Grid.Row="1" Grid.Column="1" x:Name="txtEmail"/>
                <TextBlock Grid.Row="1" Grid.Column="2" Text="Teléfono"/>
                <TextBox   Grid.Row="1" Grid.Column="3" x:Name="txtPhone"/>

                <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="4" Orientation="Horizontal"
                            HorizontalAlignment="Right" Margin="0,8,0,2">
                    <Button x:Name="btnSave" Style="{StaticResource PrimaryButton}" Content="Guardar" Click="btnSave_Click"/>
                    <Button x:Name="btnClearForm" Content="Limpiar" Margin="8,0,0,0" Click="btnClearForm_Click"/>
                    <Button x:Name="btnDelete" Content="Eliminar" Margin="8,0,0,0" Click="btnDelete_Click"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Margin="0,8,0,0">
            <Button x:Name="btnFirst" Content="|&lt;" Click="btnFirst_Click"/>
            <Button x:Name="btnPrev"  Content="&lt;"  Margin="4,0,0,0" Click="btnPrev_Click"/>
            <Button x:Name="btnNext"  Content="&gt;"  Margin="4,0,0,0" Click="btnNext_Click"/>
            <Button x:Name="btnLast"  Content="&gt;|" Margin="4,0,0,0" Click="btnLast_Click"/>
            <TextBlock x:Name="lblPage" VerticalAlignment="Center" Margin="16,0,0,0" Foreground="Gray"/>
        </StackPanel>

        <GroupBox Header="Listado de proveedores" Padding="8">
        <DataGrid x:Name="dgData"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  CanUserAddRows="False" CanUserDeleteRows="False"
                  HeadersVisibility="Column" SelectionMode="Single"
                  GridLinesVisibility="Horizontal" RowHeaderWidth="0"
                  Background="White" RowBackground="White" BorderThickness="0"
                  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                  SelectionChanged="dgData_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Número Documento" Width="1.2*" Binding="{Binding Document}"/>
                <DataGridTextColumn Header="Razón Social" Width="2*" Binding="{Binding CompanyName}"/>
                <DataGridTextColumn Header="Correo" Width="1.7*" Binding="{Binding Email}"/>
                <DataGridTextColumn Header="Teléfono" Width="1*" Binding="{Binding Phone}"/>
            </DataGrid.Columns>
        </DataGrid>
        </GroupBox>
    </DockPanel>
</UserControl>
