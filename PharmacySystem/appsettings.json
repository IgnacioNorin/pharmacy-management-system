{
  // La cadena de conexión real va en appsettings.Local.json (ignorado por git) o en la
  // variable de entorno ConnectionStrings__connection.
  "ConnectionStrings": {
    "connection": ""
  },
  // Nivel mínimo de registro en error.log: Trace | Debug | Information | Warning | Error | Critical.
  "Logging": {
    "LogLevel": {
      "Default": "Information"
    }
  }
}
